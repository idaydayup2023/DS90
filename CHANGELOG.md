# Changelog

本文件记录 `subtrans` 每个正式发布版本面向用户的变化。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [3.3.1] - 2026-09-03

### Fixed

- 翻译和审校响应兼容不同大小及版本模型的安全 JSON 变体，包括代码块、裸数组、单条对象、数字字符串索引、常见译文字段别名和附加元数据；程序仍严格拒绝空译文、缺失、重复及越界索引。
- TranslateGemma 4B 回传输入中的 `translate` 标记时，不再将已有中文的整批响应误判为失败并逐条重复请求。

## [3.3.0] - 2026-09-01

### Added

- 字幕 manifest 新增逐条质量现场记录，保存首轮候选、触发原因、错误、审校最终译文以及翻译/审校模型；正式字幕保持干净，人工检索和后续自动化返修均以日志为准。

### Changed

- 默认翻译模型由 `translategemma:12b` 切换为 `translategemma:4b`。
- 翻译流程收敛为“首轮翻译 + 一次最终审校”两步；审校模型发现问题时直接给出最终中文，不再回送翻译模型或重复审校。
- 首轮请求失败、英文回显或中文比例异常的条目必须由审校模型给出合格中文，否则该片字幕不会发布；不再以单行英文作为成功兜底。
- FTP 客户端依赖升级到 `suppaftp 10.0.2`，修复控制通道参数的 CRLF 注入漏洞。

## [3.2.1] - 2026-08-14

### Added

- 新增 `subtrans migrate apply --allow-pending`：仍须批准完整计划哈希，但只执行已就绪项目，并原样保留带下载标记或其他待确认原因的项目。
- 迁移执行日志新增逐文件源路径、目标路径、字节数、SHA-256、目标验证、源删除和整项提交记录，便于真实 FTP 操作审计与中断恢复。

### Changed

- 不完整下载产生的预期 `pending` 状态不再全局阻断同一源目录中已经完成的其他影片；实际失败仍会安全阻断后续阶段。
- 翻译模型返回格式异常时递归缩小批次到单条；单条在配置次数内始终失败时仅保留该条英文，其余字幕继续翻译、审校和发布。
- FTP 字幕发布后增加有限的只读就绪重试，每次仍完整校验视频身份、源字幕哈希和输出字幕哈希。
- 字幕缓存失配和产物未就绪现在输出具体原因，包括部分发布、清单格式、状态、视频身份、源文件及内容哈希差异。

### Fixed

- 修复群晖 FTP 对含方括号文件名的 `MLST`/目录查询异常导致已存在字幕、海报或视频被误判为缺失的问题，改用精确 `SIZE` 与父目录列表回退。
- 修复目标父目录尚未创建时 FTP `550` 被误判为存储故障的问题；此场景现在正确视为目标不存在。
- 修复群晖对部分目标返回 0 字节或不可用元数据时迁移被错误中止的问题；程序会验证文件存在性并以完整 SHA-256 作为最终依据。
- 修复服务器端移动已完成、但随后元数据查询失败时无法恢复的问题；重试会识别并校验已提升目标，不覆盖、不重复移动。
- 增加迁移待处理隔离、FTP 元数据回退、字幕单条英文保留和后续影片继续发布的回归测试。

## [3.2.0] - 2026-08-13

### Added

- 新增基于 TMDB 的电影/电视剧资料准备阶段，生成 Infuse 可识别的同名海报、背景图、NFO 和可校验清单。
- 新增 `subtrans metadata` 递归目录命令，支持安全默认模式、保留现有文件的 `--supplement` 补档和显式覆盖的 `--force` 更新。
- 支持按源相对路径配置精确 TMDB ID，自动匹配不确定时停止并给出覆盖配置提示。

### Changed

- 资料清单现在是字幕翻译和迁移计划的前置门禁；迁移会同时携带海报、背景图、NFO 和资料清单。
- 配置只保存 TMDB Token 的环境变量名，不保存 Token 明文；资料缓存有效期限制为最多 180 天。
- TMDB Token 优先读取进程/当前用户 launchd 环境；macOS 环境缺失时从登录钥匙串读取，完全缺失时输出获取、设置、持久保存和验证步骤。

## [3.1.1] - 2026-08-12

### Added

- 当外置和内置英文字幕都不存在时，调用配置的 ASR worker；未配置 ASR 时给出可执行的安装、模型和配置指引。
- 为 ffmpeg、ffprobe、Ollama、ASR/OCR worker 及 Python 模块缺失增加详细的命令行诊断信息。
- 新增审校最大尝试次数 `translation.alignment_max_attempts`，默认值为 3。

### Changed

- 没有内置英文字幕时，采用已经通过 UTF-8、SRT 结构和英语内容质量检查的外置英文字幕。
- 审校服务失败、返回无法解析或语境无法判定时记录警告并继续发布，不再阻断已经通过硬质量门的字幕。
- 同一字幕编号连续 3 次仍被审校模型否决时，仅该条保留英文原文，其余字幕继续翻译和发布。
- 内置英文文本流提取会依次尝试所有候选流，并在全部失败时汇总各流错误。

## [3.1.0] - 2026-08-12

### Added

- 发布 Rust-first 的 `subtrans` 单文件命令行程序，核心范围为字幕翻译和安全目录迁移。
- 支持 FTP 源和目标存储、原子上传、迁移计划哈希批准、断点状态以及四类可配置媒体目录布局。
- 支持本地目录纯字幕翻译模式，不启用目录迁移。
- 支持带只读前导语境的批量翻译、逐条索引绑定、术语一致性校对和对应性审校。
- 支持内置文本字幕、PGS OCR 和 ASR 外部 worker，并将 worker 命令与版本纳入缓存身份。
- 提供 Apple Silicon arm64 Release 二进制和 SHA-256 校验文件。

### Changed

- 目录迁移分类改为可审计的确定性规则；`4K`、`UHD`、`2160p`、`HEVC`、`HDR` 和 `DV` 仅作为画质证据，不决定电影或电视剧类型。
- 不再依赖 NFS 自动挂载；Apple Silicon 执行端通过 FTP 访问群晖媒体。
- 翻译失败只影响当前视频，后续视频继续处理；有效翻译产物可安全复用。

## [3.0.0] - 2026-08-09

### Added

- 将执行程序统一为 Rust `subtrans`，将范围收敛为字幕翻译与目录迁移。
- 新增严格 TOML 配置、环境变量密钥、local/FTP 存储抽象和 SQLite 可恢复状态。
- 新增严格 SRT、结构化批次翻译、输出质量门、参数完整缓存与原子发布。
- 新增可解释规则分类、不可变迁移计划、计划哈希批准、源指纹、冲突检测、SHA-256 校验与安全提交。
- 新增 ffprobe 字幕轨选择、ASR/PGS 受控 worker、生成源缓存和外部进程超时。

### Removed

- 删除运行时自动安装、Python 核心依赖、剧情生成、IMDB 泛化补全、旧智能体/MCP 包装及重复入口。

> `3.0.0` 是 V3 开发基线记录，未创建对应的 GitHub Release；正式 V3 Release 从 `3.1.0` 开始。

[Unreleased]: https://github.com/idaydayup2023/DS90/compare/v3.2.1...HEAD
[3.2.1]: https://github.com/idaydayup2023/DS90/compare/v3.2.0...v3.2.1
[3.2.0]: https://github.com/idaydayup2023/DS90/compare/v3.1.1...v3.2.0
[3.1.1]: https://github.com/idaydayup2023/DS90/compare/v3.1.0...v3.1.1
[3.1.0]: https://github.com/idaydayup2023/DS90/releases/tag/v3.1.0
