# 目录迁移与命名规范化工具 PRD（LLM 驱动）

## 1. 背景与目标

在媒体库整理过程中，下载目录（通常为 FTP 的 `/Downloads` 及其子目录）会积累大量视频文件与字幕文件，文件名风格不一致（发布组、源、编码、音轨等字段缺失或乱序），且目录结构不符合统一归档规则。

本工具目标：

- 扫描“源目录”下的视频与其伴随字幕文件（sidecar subtitles）
- 使用 LLM 识别文件名语义并输出“规范化字段”
- 将文件名规范化为“字段用 `.` 分割”的统一格式（目录名默认与文件名一致）
- 按分辨率/电影或剧集/年份分桶等规则迁移到“目标目录”
- 支持 dry-run 预览与安全迁移（避免误移动/覆盖）

## 2. 范围

### 2.1 处理对象

- 视频文件：`.mkv .mp4 .avi .mov .m4v .ts`（可配置）
- 字幕文件：`.srt .ass .ssa .vtt`（可配置）
  - 典型命名：`basename.srt`、`basename.en.srt`、`basename.zh.srt`、`basename.zh-Hans.srt` 等

### 2.2 不在本期范围

- 媒体信息探测（如从视频流内解析真实分辨率/音轨/codec）作为默认依赖（可作为增强项）
- 自动下载缺失字幕/元数据
- 跨协议复制（例如 FTP -> 本地 -> FTP）复杂链路；本期以“同一存储内 rename/move”为主（可抽象为 MCP）

## 3. 核心定义

- **规范化文件名（Normalized Basename）**：去掉扩展名后的主文件名，由若干字段用 `.` 连接构成。
- **默认目录名**：与规范化文件名一致（即目标目录为 `.../<Normalized Basename>/`，目录内包含 `Normalized Basename.<ext>` 与字幕等文件）。
- **电影（Movie）**：单个作品，通常包含年份字段。
- **剧集（TV Episode）**：包含季/集信息（Sxx / SxxEyy / 1x02 等）。
- **系列归并（Franchise Grouping）**：对衍生剧（例如 `NCIS: Miami`）归并到主系列根目录（`NCIS`）。

## 4. 规范化命名规则（输出格式）

### 4.1 字段分隔

- 字段之间使用 `.` 分割
- 字段中间不得出现空格；标题中的空格替换为 `.`
- 仅允许字符集：`A-Z a-z 0-9 . - _ :`（最终落地可进一步收敛为不含 `:`，用 `.` 替换）

### 4.2 建议字段集合（按常见发布组织习惯）

工具不强制所有字段都存在，但推荐按下述“顺序模板”输出：

#### Movie 推荐模板

`Title.Year.Resolution.Source.Codec.Audio-Group`

示例：

- `The.Thin.Red.Line.1998.1080p.BluRay.HEVC.5.1-BONE`
- `Dune.Part.Two.2024.2160p.WEB-DL.HEVC.Atmos-FLUX`

#### TV Episode 推荐模板

`Series.SxxEyy.EpisodeTitle.Resolution.Source.Codec.Audio-Group`

当缺少 EpisodeTitle 时可省略：

- `NCIS.S21E05.1080p.WEB-DL.H264.AAC-Group`
- `NCIS.Miami.S02E01.2160p.WEB-DL.HEVC.Atmos-Group`

说明：

- `SxxEyy` 为必需（若仅识别到 `Sxx`，应尝试补全 `Eyy`；补不出则输出 `SxxE00` 并标记低置信度）
- 如果原始只包含 `Sxx`，目录名仍必须包含 `Sxx`（见目录规则）

### 4.3 扩展名与文件集合

迁移时保持扩展名不变：

- 视频：`<Normalized Basename>.<video_ext>`
- 字幕：尽可能保持语言后缀：
  - `basename.en.srt` → `<Normalized Basename>.en.srt`
  - `basename.zh-Hans.srt` → `<Normalized Basename>.zh-Hans.srt`
  - 无语言后缀：`basename.srt` → `<Normalized Basename>.srt`

## 5. 目录归档规则（目标目录）

设目标根目录为 `DEST_ROOT`，规则如下。

### 5.1 电影（1080p）

- 归类到：`DEST_ROOT/X-Movie/<YearBucket>/`
- **YearBucket 规则**：
  - `Year >= 2024`：按年建目录，如 `2024/`、`2025/`
  - `Year < 2024`：按年代（每 10 年）建目录，如 `1990s/`、`2000s/`、`2010s/`

最终落点（默认目录名与文件名一致）：

`DEST_ROOT/X-Movie/<YearBucket>/<Normalized Basename>/<Normalized Basename>.<ext>`

### 5.2 电影（4K / 2160p）

- 归类到：`DEST_ROOT/MOVIE/<DecadeBucket>/`
- **DecadeBucket 规则**：始终按年代（每 10 年）建目录，如 `2020s/`、`2010s/`

最终落点：

`DEST_ROOT/MOVIE/<DecadeBucket>/<Normalized Basename>/<Normalized Basename>.<ext>`

### 5.3 剧集（1080p）

- 归类到：`DEST_ROOT/X-TV/<SeriesBucket>/`
- **SeriesBucket 规则**：
  - 使用 LLM 识别的 `franchise_root`（若存在）作为主目录名（例：`NCIS`）
  - 否则使用 `series`（例：`Breaking.Bad`）
  - 片名中空格用 `.` 代替

由于“目录名需包含 Sxx”，默认采取“季目录 + 剧集目录”两级结构：

`DEST_ROOT/X-TV/<SeriesBucket>/Sxx/<Normalized Basename>/<Normalized Basename>.<ext>`

其中 `Sxx` 来自解析出的季信息（必须存在）。

### 5.4 剧集（4K / 2160p）

- 归类到：`DEST_ROOT/TV/<SeriesBucket>/`
- 规则同 1080p 剧集，仅顶层目录不同：

`DEST_ROOT/TV/<SeriesBucket>/Sxx/<Normalized Basename>/<Normalized Basename>.<ext>`

## 6. LLM 识别与输出约束

### 6.1 输入

对每个“视频主文件”调用 LLM，输入包含：

- 原始文件名（不含路径，可含扩展名）
- 可选：同目录字幕文件列表（仅文件名），用于辅助判断剧集/语言等

### 6.2 输出（强制 JSON）

LLM 必须输出单个 JSON（无额外文本），字段建议如下：

```json
{
  "kind": "movie|tv",
  "title": "The Thin Red Line",
  "series": "NCIS: Miami",
  "franchise_root": "NCIS",
  "year": 1998,
  "season": 21,
  "episode": 5,
  "episode_title": "Episode Title",
  "resolution": "1080p|2160p|720p|unknown",
  "source": "BluRay|WEB-DL|WEBRip|HDTV|DVDRip|unknown",
  "codec": "HEVC|H264|AV1|unknown",
  "audio": "5.1|7.1|AAC|Atmos|unknown",
  "group": "BONE",
  "confidence": 0.0
}
```

说明：

- `kind` 必需
- Movie 必须尽量给出 `title` 与 `year`
- TV 必须尽量给出 `series`、`season`，并尽量给出 `episode`
- `franchise_root` 用于衍生剧归并（例如 `NCIS: Miami` → `NCIS`）
- `confidence` 用于后续降级策略（低置信度可要求人工复核/只做预览不迁移）

### 6.3 Prompt 要点

- 明确要求“只输出 JSON，不要解释”
- 提供若干示例（movie/tv、带发布组、带冒号/衍生剧）
- 对于 `:` 或特殊符号，要求在 `title/series` 中保留语义，但最终规范化时转为 `.` 或删除

## 7. 迁移流程（工作流）

1. **扫描**：遍历源目录，收集视频文件集合
2. **关联**：为每个视频文件找到“同目录、同 stem”的字幕文件集合
3. **识别**：调用 LLM 得到结构化字段（必要时重试/降级）
4. **规范化**：将字段拼装为 `Normalized Basename`（按模板与可用字段）
5. **规划**：计算目标路径（目录规则），生成迁移计划（Plan）
6. **执行**：
   - `dry-run`：只输出 Plan，不移动
   - `apply`：执行移动/重命名（要求原子性与可回滚策略）
7. **报告**：输出统计（成功/跳过/冲突/失败）与失败原因列表

## 8. 冲突与安全策略

### 8.1 同名冲突

当目标目录已存在同名目录或同名文件：

- 默认：不覆盖，标记为 `CONFLICT` 并跳过
- 可选配置：
  - `--on-conflict suffix`：追加 `-1/-2` 后缀
  - `--on-conflict overwrite`：覆盖（高风险，不建议默认）

### 8.2 幂等性

- 重复运行应尽量不重复移动同一文件
- 对已在目标目录且命名符合规范的文件：标记 `SKIPPED`

### 8.3 失败回滚

- 单个“媒体条目”（视频+字幕集合）必须作为事务：要么全部移动成功，要么全部保留原地（或记录可恢复信息）

## 9. 可观测性与记录

- 日志：
  - 每个条目输出：源路径、识别结果摘要、目标路径、执行结果
  - 禁止输出密码等敏感信息
- 状态库（可选，建议 SQLite）：
  - `items`：source_id、source_path、normalized_name、dest_path、status、error、updated_at
  - 支持断点续跑与失败重试

## 10. 配置与 CLI（建议）

### 10.1 配置文件（JSON）

建议与现有工具风格一致：

- `source.root_path`（如 FTP `/Downloads`）
- `dest.root_path`
- `video.extensions`
- `subtitle.extensions`
- `llm.base_url / model / timeout / temperature`
- `workers`（并行识别/迁移）
- `dry_run` 默认 true（安全优先）

### 10.2 CLI（建议）

- `--config <path>`
- `--once`（单次扫描执行）
- `--dry-run`
- `--apply`
- `--limit N`（只处理前 N 个视频，便于小规模验收）
- `--force`（重新识别/重命名，即使目标已存在；需结合冲突策略）

## 11. 验收标准

- 能对至少 100 个混合样本（电影/剧集、1080p/2160p、多语言字幕）生成可读的迁移 Plan
- `--dry-run` 输出的目标路径符合目录规则
- `--apply` 执行后：
  - 视频与字幕文件都在目标目录
  - 视频与字幕的 basename 一致且符合规范化命名
  - TV 目录中包含 `Sxx`
- 发生冲突时默认不覆盖，并在报告中明确列出
- 可重复运行且不产生重复迁移/破坏性操作

## 12. 风险与后续增强

- LLM 误判（年份/分辨率/剧集信息）：
  - 增强：对低 `confidence` 自动降级为“只生成 Plan，不 apply”
  - 增强：引入 ffprobe 校验分辨率、从媒体流校正字段
- 衍生剧归并不全：
  - 增强：维护本地 `franchise_map.json`，优先于 LLM
- 字幕语言后缀多样：
  - 增强：更完整的语言 tag 识别（BCP-47 优先，兼容 `.chs/.cht/.en/.eng`）

