# srt_translate PRD（字幕翻译工具）

## 1. 背景与问题

用户本地有一台可运行 Ollama 的机器，但待处理的电影/电视剧视频文件存放在 FTP 服务器上。希望自动化完成：

1) 只处理 FTP `/Downloads` 目录及其递归子目录内的视频文件  
2) 优先复用已有字幕；没有则从内置字幕/外置字幕/ASR 逐级兜底获取英文字幕  
3) 使用本地大模型（Ollama，默认 `tranlategemma`）生成高质量中英双语字幕，且保持逐条对齐  
4) 产物字幕与视频同目录保存：`.emb.srt`、`.asr.srt`、`.ai.srt`

## 2. 目标（Goals）

- 在不依赖云端服务的前提下（除 FTP 读取外），全自动为视频生成对齐的中英双语字幕 `.ai.srt`
- 能在多种字幕来源下稳定工作，并通过“字幕质量评分”在多候选英文字幕中选择最佳
- 翻译具备“场景语境感知”和“全片术语一致性”（人名/地名/专有名词/缩写前后一致）
- 输出严格遵循 SRT 格式，并保证每条英文字幕对应一条中文翻译

## 3. 非目标（Non-Goals）

- 不实现视频在线播放/媒体库 UI（本期以 CLI/守护进程为主）
- 不追求 100% 的逐字翻译与文学润色，重点是观影可用、术语一致、对齐稳定
- 不承诺对“图片型内置字幕”（PGS/VobSub）一定可用；若未开启 OCR，将视为不可提取字幕

## 4. 典型用户与使用场景

- 个人用户：定期把新下载的电影/剧集放到 FTP `/Downloads`，工具自动生成双语字幕并同目录写回
- 高频场景：连续剧多集批处理；同一剧集不同文件命名习惯；同目录可能存在多个字幕版本

## 5. 术语与文件命名约定

- **视频基名（basename）**：去掉扩展名后的文件名，如 `S01E01.1080p`  
- **内置字幕（embedded）**：封装在视频容器（mkv/mp4）中的字幕轨  
- **外置字幕（external）**：同目录下与视频同名/约定命名的 `.srt` 文件  
- **字幕产物**
  - `basename.emb.srt`：从内置字幕提取得到的英文字幕
  - `basename.asr.srt`：Whisper ASR 生成的英文字幕
  - `basename.ai.srt`：最终生成的中英双语字幕（每条中文在上、英文在下）

## 6. 输入与输出

### 6.1 输入

- FTP 服务器：包含 `/Downloads/**` 下的视频文件与可能存在的外置字幕文件
- Ollama 本地模型：默认 `tranlategemma`（允许配置覆盖）
- Whisper 本地 ASR：用于无字幕时兜底生成 `.asr.srt`

### 6.2 输出

- 与视频同目录写回：
  - `basename.emb.srt`（当内置字幕成功提取且采用该路径）
  - `basename.asr.srt`（当触发 ASR）
  - `basename.ai.srt`（总会生成，除非发生不可恢复错误）
- 可选本地状态与日志：
  - 处理状态数据库/索引（用于增量与断点续跑）
  - 日志文件（脱敏）

## 7. 总体工作流（端到端）

1) **扫描**：连接 FTP，枚举 `/Downloads` 下所有视频文件（递归）
2) **字幕获取**：对每个视频按优先级确定“英文源字幕（source.srt）”
3) **翻译与统一**：对源字幕做场景切分、术语抽取、批量翻译、全片一致性校正
4) **写回**：生成 `.ai.srt`（以及必要的 `.emb.srt/.asr.srt`）写回到视频同目录
5) **记录**：落库/写索引，避免重复处理，并支持重试

## 7.1 架构设计（Agent / Skill / MCP）

本项目建议采用“多 Agent 协作 + Skill 管理提示词/规则 + MCP 统一工具访问”的架构，以便：

- 将扫描/字幕提取/ASR/翻译/一致性/写回解耦，便于并行与故障隔离
- 把易迭代的提示词与翻译规范沉淀为可复用的 Skill，避免散落在代码里
- 把 FTP、媒体处理、ASR、Ollama、状态存储等外部能力统一抽象为 MCP 工具，降低耦合并提升可测试性

### 7.1.1 Agent 划分（职责边界）

- **Orchestrator Agent（编排器）**
  - 维护任务队列与状态机（pending/running/done/failed）
  - 决定每个视频的处理路径（emb/external/asr），并把子任务投递到不同 worker
  - 控制并发与限流：翻译与 ASR 各自独立的 worker 池，互不阻塞
- **Scanner Agent**
  - 负责 FTP `/Downloads/**` 的目录枚举与变更发现（轮询或基于增量索引）
- **Subtitle Acquisition Agent**
  - 负责内置字幕探测/提取、外置字幕收集、质量评分与择优
  - 输出确定的英文源字幕（本地缓存路径或远端写回路径）
- **ASR Agent（Whisper Worker Pool）**
  - 只处理“需要 ASR 的视频”，并行运行，产出 `basename.asr.srt`
  - 通过状态回调/事件通知 Orchestrator：ASR 完成后再进入翻译队列
- **Translation Agent（Ollama Worker Pool）**
  - 场景切分、实体抽取、翻译（批量/逐条）与对齐校验
  - 生成 `.ai.srt` 草稿与一致性后处理版本
- **Writer Agent**
  - 负责把 `.emb.srt/.asr.srt/.ai.srt` 写回到 FTP 同目录（可带原子写/临时文件策略）
- **Observer/Telemetry Agent（可选）**
  - 汇总日志、指标与可解释评分，便于调优与排障

### 7.1.2 任务模型（建议的消息/数据结构）

以“可持久化的任务对象”在各 Agent 间传递，建议最小字段：

- `video_id`：稳定标识（FTP 路径 + 文件大小 + mtime 的 hash）
- `video_path`：远端路径
- `local_cache_path`：本地缓存路径（如需下载处理）
- `source_subtitle`：选定英文源字幕的信息（类型 emb/external/asr、路径、评分）
- `artifacts`：已生成的产物列表（emb/asr/ai 的远端路径与校验信息）
- `error`：失败原因（可重试/不可重试）

### 7.1.3 Skill（提示词/规则资产化）

Skill 的定位：把“高频迭代、对效果影响大、但不应硬编码在业务逻辑中的内容”独立成资产，包括：

- TranslateGemma 官方 Prompt 模板（见 10.1.1）
- 场景切分提示词/策略阈值（10.2）
- 全片实体抽取与统一规则（10.3、10.5）
- 批量翻译对齐的输出约束（10.4）
- 一致性后处理的检查清单（10.5）

实现建议：

- Skill 以版本化文本/模板文件存在（便于 A/B 与回滚）
- Translation Agent 只引用 Skill 名称与版本，不直接拼接散落的提示词片段

### 7.1.4 MCP（工具与外部能力抽象层）

把所有“副作用/外部依赖”统一收敛到 MCP 工具层，业务 Agent 只做编排与决策。

建议 MCP server 能力划分（可合并部署）：

- **FTP MCP**：`list`, `stat`, `download`, `upload`, `mkdir`, `atomic_write`
- **Media MCP**（ffprobe/ffmpeg 封装）：`probe_subtitles`, `extract_subtitle_track`, `extract_audio`
- **ASR MCP**（whisper 封装）：`transcribe_to_srt`（支持 job 模式与进度）
- **Ollama MCP**：`generate`（支持超时、重试、并发限制、模型切换）
- **Store MCP**（sqlite/kv）：`get_task`, `put_task`, `list_pending`, `mark_done`

关键要求：

- MCP 需要返回结构化结果与错误码，便于 Orchestrator 做“可重试/不可重试”判定
- MCP 需要内置限流与资源保护（尤其是 ASR/Media 的 CPU/GPU/IO）

## 8. 视频扫描与过滤规则

### 8.1 扫描范围

- 固定根目录：FTP `/Downloads`
- 递归子目录：包含所有层级

### 8.2 视频文件识别

- 默认识别扩展名（可配置）：`.mkv .mp4 .avi .mov .m4v .ts`
- 忽略：
  - 体积过小（可配置阈值，如 < 50MB）的文件
  - 临时/不完整文件（如扩展名 `.part`、或同目录存在下载中标记）

### 8.3 增量策略

- 若同目录已存在 `basename.ai.srt` 且满足“未过期”规则则跳过；可通过 `--force` 强制重译
- “未过期”建议规则（可配置）：
  - `ai.srt` 的修改时间 >= 选定源字幕（`.emb.srt/.asr.srt/外置字幕`）修改时间

## 9. 字幕获取流程（优先级 + 质量评分）

### 9.1 决策总览

按下列优先级依次尝试，命中即停止：

1) **同名内置提取产物存在**：若同目录存在 `basename.emb.srt`，直接作为英文源字幕  
2) **提取视频内置字幕**：从容器中选择质量最佳的英文字幕轨，提取并保存为 `basename.emb.srt`  
3) **查找外置字幕**：同目录下查找同名 `.srt` / `.en.srt` 候选，选择质量最佳者  
4) **Whisper ASR**：对视频音轨做 ASR，保存为 `basename.asr.srt` 作为英文源字幕

### 9.2 内置字幕提取要求

- 需要先探测字幕轨信息（语言、编码、字幕类型、标题、默认/强制标记）
- 英文字幕轨识别策略（综合判断）：
  - `language` 为 `eng/en`，或 title/handler 含 `English/ENG`
  - 若存在 `SDH/HI`（听障字幕）标记，降低优先级但不直接排除
  - 若字幕类型为文本型（SubRip/ASS/WebVTT 等）优先；图片型（PGS/VobSub）在未开启 OCR 时视为不可用
- 提取输出统一转换为 SRT（UTF-8），并命名为 `basename.emb.srt`

### 9.3 外置字幕查找规则

在视频同目录下，按以下模式收集候选（可配置扩展）：

- `basename.srt`
- `basename.en.srt`
- `basename.*.srt`（例如存在多个版本：`basename.1.srt / basename.eng.srt`）

若同时存在多个候选，使用质量评分择优；若评分相近，可用“最近修改时间更晚”作为次级排序。

### 9.4 Whisper ASR 兜底规则

触发条件：内置字幕与外置字幕均无法获得可用英文字幕。

- ASR 输出文件：`basename.asr.srt`
- 建议默认参数（可配置）：
  - 英文优先（`--language en` 或自动检测后确认）
  - 生成 SRT 分段并尽量对齐可读长度（如每条 1–7 秒，过长自动切分）
- 性能与并行建议（重要）：
  - Whisper ASR 通常是全流程最慢环节，建议以“后台任务/工作队列”方式并行执行
  - 主流程应尽量不阻塞：当某个视频进入 ASR 兜底时，任务排队或分发到独立 worker，主线程继续处理其他视频的“字幕提取/翻译/写回”
  - 并发控制需要可配置（如 `asr_workers`），并对 CPU/GPU、显存与磁盘 IO 做限流，避免拖慢 Ollama 翻译吞吐
  - 任务完成后回填：ASR 生成 `.asr.srt` 后再触发该视频的翻译与 `.ai.srt` 生成（支持断点续跑）

### 9.5 字幕质量评分（核心）

目标：当存在多个英文字幕候选时，选择“观影可用、内容完整、时轴合理、文本像人话”的那一个。

对每个候选字幕计算 `score`，由以下维度加权组成（权重可配置）：

1) **可解析性（硬门槛）**
   - SRT 结构可解析；编号递增/可容错；时间戳格式合法
2) **时轴健康度**
   - 时间戳单调递增；重叠比例低；空洞/倒退比例低；总时长覆盖率合理
3) **文本健康度**
   - 英文字母占比高、乱码占比低
   - 过多音乐符号/特效行（`♪`、`[MUSIC]`）降低分
   - 过多空行、重复行降低分
4) **可读性约束**
   - 平均每条字数、每秒字符数（CPS）在合理区间（过密/过稀均降分）
5) **覆盖度**
   - 字幕条目数过少（相对视频时长）降低分

质量评分输出需可解释（记录各维度分值），便于排障与调参。

## 10. 翻译流程（场景感知 + 术语一致 + 批量对齐）

### 10.1 总体要求

- 翻译引擎：Ollama，本地模型默认 `tranlategemma`
- 逐条对齐：每条英文字幕必须生成对应的中文翻译（1:1）
- 术语一致：人名/地名/组织/专有名词/缩写全片统一，不允许前后不同译名
- 场景差异：不同场景（医院/反恐枪战/科幻等）允许有不同术语偏好，但不得造成同一实体翻译漂移

### 10.1.1 TranslateGemma 官方 Prompt 指南（编程参考）

TranslateGemma 期望输入为“单条 user message”，并遵循如下结构（占位符由调用方替换）：

```
You are a professional {SOURCE_LANG} ({SOURCE_CODE}) to {TARGET_LANG} ({TARGET_CODE}) translator. Your goal is to accurately convey the meaning and nuances of the original {SOURCE_LANG} text while adhering to {TARGET_LANG} grammar, vocabulary, and cultural sensitivities.
Produce only the {TARGET_LANG} translation, without any additional explanations or commentary. Please translate the following {SOURCE_LANG} text into {TARGET_LANG}:


{TEXT}
```

关键点：

- `{TEXT}` 前必须有两个空行（two blank lines）
- 输出必须只有目标语言译文，不要输出解释、注释、前后缀、代码块标记等
- 推荐以“单段文本→单段译文”的方式调用（与官方提示词预期一致）

示例（English → Chinese Simplified）：

```
You are a professional English (en) to Chinese (zh-Hans) translator. Your goal is to accurately convey the meaning and nuances of the original English text while adhering to Chinese grammar, vocabulary, and cultural sensitivities.
Produce only the Chinese translation, without any additional explanations or commentary. Please translate the following English text into Chinese:


Hello, how are you?
```

与本项目“批量翻译 + 逐条对齐”的关系：

- TranslateGemma 官方提示词强调“只输出译文”，这与“要求模型输出 JSON/带索引结构化结果”的指令存在天然冲突
- 若坚持使用 TranslateGemma 且需 1:1 对齐，建议实现上采用“逐条翻译”或“批量输入但用外部可验证的切分方案并进行严格对齐校验与失败重试”
- 若需要模型直接输出严格结构化结果（如 JSON 数组）以简化对齐，建议允许配置切换到更擅长指令遵循/结构化输出的模型

### 10.2 场景切分（Scene Segmentation）

目的：为翻译提供局部语境，降低跨场景误用术语。

建议切分策略（可组合）：

- 基于时间间隔：相邻字幕间隔 > 阈值（如 10–20 秒）视为新场景候选
- 基于语义变化：连续字幕主题/地点变化显著（由 LLM 对一段字幕摘要判断）
- 基于章节信号：出现明显的地点/时间提示（如 “ER”, “Operation Room”, “基地”, “三天后”）

切分输出：场景列表，每个场景包含若干条字幕索引范围（如 `120–188`）。

### 10.3 全片实体抽取与统一（Entity & Glossary）

在正式翻译前，对源字幕做一次全局分析，生成：

- **实体表（Entity Map）**：人名、地名、组织、武器型号、药物/疾病、科技名词等
- **缩写表（Abbreviation Map）**：如医院场景常见缩写（BP, ICU, CT），军警场景（IED, ROE），科幻（FTL, AI 等）
- **统一译名规则**：
  - 同一英文实体在全片必须映射到唯一中文译名
  - 若实体首次出现时不确定，可记录为“待定”并在后续出现更多上下文后回填，但最终输出必须一致

### 10.4 批量翻译与对齐策略（Batch + Alignment）

目标：提高效率，同时保证每条字幕仍与原英文条目对应。

建议做法：

- 按场景分批：每个场景内按固定 batch size（如 20–60 条）请求翻译
- 严格结构化输出：要求模型以可解析结构输出（例如 JSON 数组），元素必须包含：
  - `index`（原字幕序号）
  - `zh`（中文翻译，单行或多行但不要包含英文）
  - 可选 `notes`（内部诊断，可关闭）
- 对齐校验：
  - 返回条目数必须与请求数一致
  - index 必须全覆盖且无重复
  - 失败则自动重试（缩小 batch、或启用逐条 fallback）

### 10.5 一致性后处理（Consistency Pass）

在获得初稿翻译后，进行一次一致性校正：

- 以实体表为约束，扫描全片中文翻译中实体译名，统一替换到规范译名
- 对常见缩写/术语做一致性检查（如同一缩写不应被翻成多个中文）
- 保留原英文行不改动（除非为规范化空白/控制字符）

## 11. 输出字幕格式规范（.ai.srt）

### 11.1 命名与位置

- 文件名：`basename.ai.srt`
- 保存位置：与视频文件同目录（FTP `/Downloads/.../`）

### 11.2 编码与换行

- 编码：UTF-8（建议无 BOM）
- 换行：`\n`（跨平台容错）

### 11.3 每条字幕内容格式（强制）

每个 SRT block 必须为：

1) 序号（与源字幕一致或重新编号，但必须递增）
2) 时间戳：`HH:MM:SS,mmm --> HH:MM:SS,mmm`
3) **中文行**
4) **英文行**
5) 空行分隔

示例（仅展示格式）：

```
128
00:12:34,567 --> 00:12:36,100
我们得立刻离开这里。
We need to leave now.

```

## 12. 配置与运行方式

### 12.1 配置项（建议）

- FTP
  - `host`, `port`, `username`, `password`/token（支持环境变量注入）
  - `root_path`（默认 `/Downloads`）
  - `concurrency`（并发目录/文件列举与下载）
  - `local_cache_dir`（用于临时下载与处理）
- 处理策略
  - `video_extensions`
  - `skip_if_ai_exists`、`force`
  - `subtitle_quality_weights`
  - `scene_gap_seconds`
  - `batch_size`
- 依赖工具路径
  - `ffmpeg`, `ffprobe`
  - `whisper`（或等价实现）
  - `ollama`（或其 HTTP endpoint）
- Ollama
  - `model`（默认 `tranlategemma`）
  - `temperature`、`top_p`（建议偏确定性）
  - `max_tokens` / `context_window`（视模型能力）

### 12.2 运行模式

- CLI 单次运行：扫描并处理一次
- 守护模式：定时轮询 FTP（如每 10 分钟），发现新视频就处理

## 13. 错误处理与可观测性

### 13.1 错误分级

- 可重试错误：FTP 网络抖动、Ollama 超时、提取工具短暂失败
- 不可恢复错误：视频文件损坏、无音轨且无字幕、无写入权限

### 13.2 重试与降级

- 翻译失败：
  - 先缩小 batch size 重试
  - 再降级到逐条翻译
- 字幕提取失败：
  - 从内置失败降级外置
  - 外置缺失降级 ASR

### 13.3 日志与审计

- 记录每个视频的处理决策链：
  - 选择了哪类字幕来源（emb/external/asr）
  - 多候选时各自评分与最终选择原因
  - 翻译批次大小、重试次数
- 日志脱敏：FTP 密码/Token 禁止落日志

## 14. 性能与资源约束（非功能）

- 支持批处理：一次处理上百集视频不崩溃，失败不阻塞后续文件
- 并发与限流：
  - FTP 下载/探测并发可控，避免压垮服务器
  - Ollama 并发请求可控（建议队列化）
- 本地磁盘：临时缓存可自动清理；支持断点续跑

## 15. 验收标准（Acceptance Criteria）

对任意位于 FTP `/Downloads/**` 的视频文件，工具满足：

- 若存在 `basename.emb.srt`，直接使用并生成 `basename.ai.srt`
- 若不存在 `basename.emb.srt` 且视频含可用英文内置字幕，能提取并生成 `basename.emb.srt` 与 `basename.ai.srt`
- 若无可用内置字幕但存在外置字幕候选，能评分择优并生成 `basename.ai.srt`
- 若内置/外置均无，能通过 Whisper 生成 `basename.asr.srt` 并生成 `basename.ai.srt`
- `basename.ai.srt` 中每条 block 为“中文行 + 英文行”，条目数与源字幕一致且时间戳对齐不乱序
- 同一实体在同一字幕文件内译名一致（抽样检查通过）

## 16. 里程碑建议（MVP → V2）

### MVP（第一阶段）

- FTP 扫描 + 基础增量
- 字幕获取：emb 提取（仅文本型）/外置/whisper 兜底
- 字幕质量评分（基础维度）
- Ollama 批量翻译 + 结构化对齐 + 输出 `.ai.srt`
- 全片实体一致性（基础替换规则）

### V2（增强）

- 图片型字幕 OCR（PGS/VobSub）
- 更强的场景切分（语义聚类/多通道特征）
- 术语库持久化（同剧集跨集复用）
- 多语言支持（不止英文→中文）
